# Path to your Google OAuth 2.0 credentials file (downloaded from Google Cloud Console)
GOOGLE_CREDENTIALS_PATH=./credentials.json

# Path where the OAuth token will be cached after first authentication
GOOGLE_TOKEN_PATH=./token.json

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
